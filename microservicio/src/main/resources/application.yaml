server:
  port: 8001
  servlet:
    context-path: /talentoUCO

spring:

  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/talentoUCO
    username: postgres
    password: fanti
  
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: create


jwt:
  secret: fanti
